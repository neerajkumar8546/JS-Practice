// The fixed() method of String values creates a string that embeds this string in 
// a <tt> element (<tt>str</tt>), which causes this string to be displayed in a fixed-width font.

let Email="neerajk.ext@innov.in"

console.log(Email.fixed())

//Add tt tag in the string using fixed method , its is used to fixed the font width font
// of the string 