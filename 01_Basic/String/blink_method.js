// The blink() method of String values creates a string that embeds this string 
// in a <blink> element (<blink>str</blink>), which used to cause a string to blink in old browsers.

let contentString='Hello World'

console.log(contentString.blink());